# Jury claim evidence matrix

This matrix uses the exact three claim texts retrieved from the challenge's
official `claims.json` for OpenReview `jNv4sl4YZH` on 2026-07-19. The machine-
readable snapshot is `repro/configs/jury_claims.json`; the publication gate
requires all three entries and the six-point total exactly, then downloads the
live official file again and rejects any claim-count or wording drift before
creating the publication manifest.

| Jury claim | Direct reproduction evidence | Independent evidence and falsifiers | Fail-closed acceptance rule |
| --- | --- | --- | --- |
| **C1.** P2E calibrator converts conformal p-values to e-values without altering the induced prediction set | `outputs/claim1_source_crosscheck.json` invokes the pinned author implementation over all 18 finite-rank `(n, alpha)` cells. | `outputs/claim1_independent.json` independently enumerates the finite ranks, verifies identical p/e threshold membership and exact e-value expectations, and shows classic calibrators inflate every eligible set. | 18/18 source and clean-room membership identities; every exact expectation passes; all positive and inflation controls pass; documented source underflow cannot change membership. |
| **C2.** Yields substantial efficiency gains over existing p-to-e methods in conformal inference | Four released OpenML tasks, 20 fixed seeds, 24 methods, `M=512`, and `B=500`, producing exactly 1,920 raw CA cells, plus the complete 11,700-cell CCP protocol shared with C3. | `outputs/claim2_independent.json` reconstructs CA coverage/length directly from raw cells and compares P2E with log, square-root, and linear calibrators in matched WECA and UR-WECA families. `outputs/claim3_independent.json` independently performs 36 matched CCP comparisons against AoN, log, square-root, and linear calibrators. `outputs/paper_headline_comparison.json` checks all 32 reported CA P2E/comparator cells and all 90 CCP cells tabulated in the paper. | Exact CA and CCP cell sets; no missing, duplicate, unexpected, non-finite, or out-of-domain metric rows (coverage must lie in `[0,1]`, length must be nonnegative); P2E must reduce CA length by at least the pre-final-dataset 10% materiality threshold in all 24/24 classical comparisons; in CCP it must be strictly shorter in all 36/36 comparisons, including 9/9 AoN cells, with all 27/27 classical reductions at least 10%; all 488 reported mean/SD scalars across 122 paper-table cells must remain inside the predeclared tolerances. |
| **C3.** Enables exact 1-α coverage in cross-conformal prediction and conformal aggregation | Released CCP protocol on Boston, Abalone, and Parkinson data: 100 seeds, three models, 13 methods, and the paper's `K=15/15/20`, producing exactly 11,700 raw cells. The complete CA evidence from C2 covers the second named application. | `outputs/claim3_independent.json` recomputes CCP coverage/length from every raw cell. `outputs/claim3_independent_e_merge.json` exactly enumerates rank tuples and solves sparse LPs over every joint coupling with the required uniform rank marginals in two equal-weight ECCP and six nonuniform WECA cases, for both deterministic and the paper-exact independent-uniform randomized thresholds. Five further orbit LPs enforce fold exchangeability exactly for the prefix-maximum ECCP-Exch and randomized-first UR-ECCP-Exch constructions. `outputs/weca_independence_audit.json` binds to the pinned WECA function hash, checks its disjoint three-way split, and proves selected weights invariant to final-calibration and test-data/outcome mutations in 6/6 released-seed cases; invalid scaling and forbidden outcome-adaptive weighting are falsifiers. | Exact 11,700-cell set with finite coverages in `[0,1]` and nonnegative lengths; all 27 ECCP/ECCP-Exch/UR-ECCP-Exch coverage means no more than two percentage points below nominal and all reported ECCP mean/SD cells inside headline tolerance; every valid fixed-weight coupling has deterministic and randomized worst-case failure probability at most `alpha`; all 5/5 exchangeable prefix cases pass for both variants; all six source-bound WECA noninterference cases pass; adaptive weighting violates both weighted-merge LP bounds in 8/8 cases and changes under the separate test-adaptive control in 6/6. |

The empirical coverage estimates check the released applications and headline
numbers; the exact rank enumeration and adversarial-coupling LP check the
finite-sample guarantee. Neither is presented as a substitute for the other.
