# Limitations and deviations

- The universal theorem is supported by its analytic threshold identity; the
  18-cell grid is a regression of that identity, not a finite substitute for
  the quantifier.
- Exact-rank boundaries and low-level cases outside Theorem 2.6 are rejected
  rather than silently treated as theorem instances.
- Author-source floating underflow is documented and cannot change membership.
