# Method

The verifier consumes the independently validated complete CA and CCP raw-cell
aggregations. It requires exact cell sets, compares P2E directly with every
named baseline, and checks the predeclared empirical coverage tolerance.
The independent checker uses only the release summary and rejects row,
comparison, coverage, AoN, or control-count drift.
